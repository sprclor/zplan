选择器是一个逻辑上的集合，它可以选择



```
People[20] a = { { "Jackie" } { "Tom" } { "Jhon" } }
let names = $( a[*].name )
println( names ) // [ "Jackie" "Tom" "Jhon" ]

stroke Bound { left right top bottom }
type Slice { padding,margin Distance-Bound }

border-top
padding-top

fn Distance-Bound {
	( ) { }
}

fn Border-Bound {
	( ) {  }
}

let a = Slice{ { 2em } { 2em } }
let b = Slice{ { 1.2em } { 1.4em } }
```

```
fn Bound <T> {
	( all T ) { }
	( x T, y T ) { }
	( left,right T, y T) { }
}

stroke Bound { left right top bottom }

type Slice { padding,margin Distance-Bound }

let s1 = Slice {}
s1.padding.left = 2em
$(s1) = {
	padding: 2rem
}
```

