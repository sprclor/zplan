## 别名

```
type People {
    run() {
        return "I can run"
    }
    jump() {
        return "I can jump"
    }
}
type Human People
jackie = new Human()
jackie.run() // I can run
jackie.jump() // I can jump
```

