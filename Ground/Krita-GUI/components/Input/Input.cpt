/
	这是一个接受用户输入的基础组件
	它监听focus和blur事件来维持actived状态
/

type {
	value float
}

struct ( ) {
	# $value
}

state {
	actived
}

user {
	$focus( ) {
		state.add(actived)
	}
	$blur( ) {
		state.rm(actived)
	}
}